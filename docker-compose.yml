services:
  postgres:
    image: postgres:16-alpine
    container_name: payguard-postgres
    environment:
      POSTGRES_DB: payguarddb
      POSTGRES_USER: payguard
      POSTGRES_PASSWORD: payguard
    ports:
      - "5432:5432"
    volumes:
      - payguard-postgres-data:/var/lib/postgresql/data

volumes:
  payguard-postgres-data: